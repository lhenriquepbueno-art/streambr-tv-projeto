<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuração de segurança de rede.

    Em desenvolvimento: permite HTTP para o servidor local (192.168.x.x)
    Em produção: remova o bloco domain-config e use apenas HTTPS.

    Android 9+ bloqueia HTTP por padrão — sem isso o app não consegue
    conectar no backend rodando em HTTP na rede local.
-->
<network-security-config>

    <!-- Em desenvolvimento: permite HTTP para qualquer endereço -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">rotasvip.net</domain>
    </domain-config>

</network-security-config>
